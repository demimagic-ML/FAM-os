# FAM_OS Master Plan v2 Completion Prompt

You are the primary implementation agent responsible for finishing the FAM_OS
engineering fabric in:

`/home/demimagic/Desktop/NewLLM/FAM_OS`

## Objective

Complete every requirement and exit gate in `MASTER_PLANv2.md` Phases 24–31
as one production-reachable, signed, installed, owner-controlled engineering
system.

Do not stop at contracts, scaffolding, component tests, fake-driven evidence,
source composition, a Console projection, or a candidate workspace. Continue
until a user can give FAM a natural-language engineering task and the installed
product can safely carry that task through the complete authorized lifecycle.

Maintain one persistent goal with this exact meaning:

> Complete and truthfully requalify MASTER_PLANv2 Phases 24–31, including the
> natural-language master engineering lifecycle, every remaining engineering
> capability, Console and Shell control surfaces, signed installed evidence,
> both hardware profiles, the final 24-hour soak, independent human review, and
> direct-evidence integration coverage.

The goal is complete only when every requirement below is proven from the
current final candidate. Do not redefine completion around the work that
already exists.

## Current Verified Baseline — 2026-07-19

Read `MASTER_PLANv2_STATUS_AUDIT.md` for the current checked/open requirement
matrix, maturity classifications, direct evidence, and external blockers.

`MASTER_PLANv2.md` currently contains 47 checked requirements and 18 unchecked
requirements. A checkmark is not automatically proof of installed end-to-end
availability.

The latest applicable architecture records are ADRs 0198–0227 and Handoffs
0231–0264. The installed evidence baseline first proved the front-half
engineering path can:

1. Interpret natural language into a non-authoritative engineering proposal.
2. Show an exact ordinary authority envelope.
3. Bind owner confirmation to the authenticated Console session.
4. Activate a task-scoped grant.
5. Persist the task definition and monotonic budget.
6. Inspect the selected repository.
7. Produce repository analysis and an architecture proposal.
8. Create an isolated candidate workspace.

The current worktree has since advanced the ordinary local path to
`installed_tested`. ADRs 0202–0204 and Handoffs 0235–0238 cover bounded
candidate context, strict untrusted model plans, Core-bound edits, trusted
signed-recipe selection,
aggregate candidate verification, the exact changeset checkpoint, transactional
apply, owner-tree reverification, replay-safe local Git commit, owner-encrypted
proposal/generation persistence, and equivalent Console/Shell checkpoints.
A fresh signed installation using a durable system interpreter directly proves
that ordinary single-repository slice through both Console and Shell, including
bounded semantic repair, no-op effect filtering, two passing verification
records, exactly one clean
commit, and committed-state recovery after service restart. Raw evidence is
`artifacts/product/phase30/natural-local-delivery-20260719-02/evidence.json`.
This is not proof of the complete Phase 30 wording. Explicit rollback and
separately approved publication are now source-composed after this installed
baseline but still require a new signed installed candidate.
Documentation/incident/review orchestration, the remaining
Phase 27 and 29 powers, and final Phase 31 qualification remain open.

The source worktree now additionally composes governed documentation,
incidents/repair/recovery, policy-selected signed independent review, and local
Phase 27.11 runtime diagnostics. ADR 0218 and Handoff 0253 bind diagnostic-only,
candidate, pristine performance-baseline, changeset, and post-apply evidence
to the same natural lifecycle and package the helper in the signed release.
Distributed service tracing and a new signed installed/both-profile proof are
still required; do not check 27.11 from source evidence.

The source worktree also composes the natural SQLite vertical slice. ADR 0219
and Handoff 0254 bind deterministic target and reversible-migration planning,
disposable forward/rollback rehearsal, encrypted candidate backup,
fresh-authority recovery, database-only candidate verification, exact binary
changeset inclusion, independent owner post-apply schema/data observation,
commit, restart reconstruction, and inverse-commit rollback to the same master
loop. Isolated PostgreSQL migration verification is now separately
`installed_component_tested`; external PostgreSQL, MySQL, and independently
enforced profile rows remain open, so do not check 27.12.

ADR 0220 and Handoff 0255 also correct natural-routing precedence and delegated
MCP terminal-result handling without widening authority. Explicit Application
Fabric context is never stolen by the repository lifecycle, while URI-only
engineering requests continue to work. Ambiguous `use` phrases require a
concrete machine target before entering the action firewall. An earlier full
source run observed one intermittent legacy Shell `core_unavailable` error;
its exact test passed twenty consecutive isolated repetitions and the next
complete source run did not reproduce it. Retain it as historical timing
evidence, not a current failing item or an owner/host prerequisite.

ADRs 0221–0223 and Handoffs 0256–0258 source-compose a narrow natural
integration vertical slice. The same master loop deterministically plans
release-signed, loopback-only static HTTP and fixed root-Python-API templates;
binds exact candidate health and cleanup receipts into the preview; waits for
owner approval; applies; runs a distinct environment against a fresh
owner-workspace clone; requires cleanup; commits; and reconstructs both
environments after restart through Console and Shell. Explicit API/backend/
full-stack wording plus a regular `api.py` can produce an API-before-static
dependency graph. It does not grant external network or secrets and does not
accept model-selected commands or recipe coordinates. A real process test
proves both services healthy and cleaned. Natural generation can now propose a
strict `fam.integration.json` document containing only logical IDs, closed
service roles, and dependencies; Core maps those roles to signed templates and
rejects intent expansion. The declaration is checkpointed, applied, committed,
and decoded again from the fresh owner clone. The latest complete source run
executes 1,845 tests with 15 fail-closed host-security failures, no internal
errors, and two skips. Treat this as source composition for two fixed templates,
not completion of 27.13, 30.1, 30.5, or 30.9. Dynamic port reservation,
browser, container, cluster, signed-installed network/secret enforcement, and
remote-database composition remain open.

Handoff 0259 and
`artifacts/product/phase30/natural-integration-install-20260719-01/evidence.json`
advance the narrow natural API/static graph to `installed_component_tested`.
Fresh signed release `phase30-natural-integration-20260719-1` installs
healthily, loads both exact integration recipes and all 415 schemas from the
installed package, rejects checkout import leakage, and passes 100 tests,
including real two-service process health and cleanup. This is not a live or
production-verifier pass: the artifact remains top-level failed because the
root-owned `fam-os-userns` profile is absent. Do not regress or overstate this
boundary.

ADR 0224 and Handoff 0260 also source-compose the separate natural
integration-resource ceremony. Explicitly labelled canonical network
destinations and opaque secret references form a second exact, owner-encrypted,
task-scoped grant with a visible byte ceiling. Console and Shell require its
own session-bound approval before the ordinary task grant. Candidate and
post-apply planning re-derive the scope from original intent and reject
expansion before using the existing network/secret enforcement services.
Generic resource words remain blocked. This branch passes 136 affected tests
and 41 architecture tests; complete source discovery runs 1,853 tests with the
same 15 external host-security failures. It is not yet signed-installed or live
broker/secret evidence.

ADR 0225 and Handoff 0261 additionally source-compose one isolated fixed
PostgreSQL service role. Natural intent and the separate resource checkpoint
bind one opaque password ref, stable `integration:postgresql` consumer, exact
256 MiB ephemeral-storage ceiling, release-owned PostgreSQL 17 image digest,
and signed health. A real Core/Docker run passes and cleans with no leftovers.
The internal Docker network produced no host binding, so the template exposes
no port and does not yet create a remote `DatabaseTarget` or run migrations.
The affected 121 tests, 41 architecture tests, and 415 schemas pass. Continue
with a broker-attested non-production PostgreSQL attachment and a real
plan/backup/apply/verify/restore executor; keep MySQL, signed installed proof,
profiles, soak, and human review open.

Handoff 0262 advances the isolated PostgreSQL service to
`installed_component_tested`. Fresh signed release
`phase30-natural-postgresql-20260719-2` passes 107 installed-package-first tests
and repeats the real fixed-image, opaque-secret, signed-health, and cleanup row
without checkout imports or leftover Docker resources. Installation diagnosis
is healthy; the top-level artifact remains false because `fam-os-userns` is
absent. Evidence:
`artifacts/product/phase30/natural-postgresql-install-20260719-01/evidence.json`.
Do not infer a brokered endpoint, SQL migration, live Console/Shell activation,
profile passage, soak, or review from this component proof.

ADR 0226 and Handoff 0263 supersede the preceding “no SQL migration” gap for
the isolated candidate service only. Fixed role `fam_migrator` is physically
non-superuser; bounded forward/reverse SQL streams to fixed `psql` argv; the
encrypted custom backup, transaction probe, exact schema/data states, repeat
forward result, fresh restore, and fresh-owner post-apply repetition are bound
to the changeset. Signed release `phase30-postgresql-migrations-20260719-3`
passes 119 installed-package-first tests and exposes 417 schemas. It provides
no external endpoint, host port, production mutation, or MySQL support.

ADR 0227 and Handoff 0264 additionally correct ordinary workspace admission.
Generated `.next`, dependency, build, cache, virtual-environment, and coverage
trees are pruned before validation and are never followed or copied. Source
symlinks/hardlinks and candidate links remain denied. The real selected
npm/Next.js project produces 138 observations and a 238-entry candidate; the
developer Console on port 8877 serves this current checkout. This source proof
still needs final signed/live requalification.

The current `configs/integration/coverage.json` records only `observe`,
`propose`, `modify`, `execute`, and the candidate workspace as
`installed_tested` from that direct artifact. The other ten engineering
authorities remain component-tested and not production-reachable. Promote a row
only after a newly built signed installed artifact directly proves its exact
scope; `operationally_proven` remains the gap-free completion state.

The user-facing process currently listening on `127.0.0.1:8765` is still the
older activated `0.1.0` installation. The signed qualification candidate above
was intentionally installed under an isolated `/tmp` prefix. Do not represent
the live Console as updated until the owner deliberately installs/activates the
new release and restarts that service.

A separate transient developer service `fam-os-natural-dev.service` currently
serves the current checkout on `localhost:8877` with isolated browser state.
Treat it as interactive source validation, not signed live-release promotion.

The open plan requirements are:

- 27.11–27.16
- 29.7–29.8
- 30.1 and 30.5–30.9
- 31.2 and 31.4–31.6

The checked requirements that still require final-candidate revalidation
include Phases 24–26, 27.1–27.10, Phase 28, 29.1–29.6, 30.2–30.4, 31.1, and
31.3. Historical source, fixture, or installed evidence may be reused as a
diagnostic baseline, but it cannot qualify newly wired behavior.

## Mandatory Startup Reconstruction

Before changing implementation:

1. Read `AGENTS.md` completely.
2. Read `MASTER_PLAN.md` completely and preserve all open Phase 0–23 gates.
3. Read `MASTER_PLANv2.md` completely.
4. Read `handoffs/README.md` and the newest applicable handoffs.
5. Read every relevant accepted ADR, beginning with ADRs 0198–0202 for the
   master loop.
6. Check `.larry/STATE` and `.larry/wiki/overview.md`.
7. Start code discovery with `larry search`; use the codebase knowledge graph
   when available before reading source.
8. Inspect the dirty worktree and preserve every unrelated user change.
9. Inspect `configs/integration/coverage.json`, the newest raw artifacts, the
   active installed release, and the running service state.
10. State the exact plan requirement currently being advanced.

Treat the current worktree and external runtime as authoritative. Older audit
documents may describe an earlier state and must not override newer direct
evidence.

## The Integration Spine: Finish 30.1 First

Use Phase 30.1 as the integration spine. Do not add another disconnected
acceptance-only service. Implement the following ordinary local-code path
through the real product composition before expanding to the remaining
specialized capabilities:

```text
natural-language request
  -> deterministic intent classification
  -> repository and toolchain observation
  -> visible proposed task envelope and budgets
  -> authenticated owner grant or exact denial
  -> durable task admission
  -> bounded repository analysis
  -> decision-complete architecture/design proposal
  -> isolated candidate workspace
  -> bounded candidate context
  -> untrusted model-generated structured change proposal
  -> Core validation and current-state digest binding
  -> typed candidate file/directory/asset operations
  -> trusted selection of installed signed recipes
  -> diagnostics/build/tests in the candidate sandbox
  -> bounded repair and re-verification, or truthful escalation
  -> coherent changeset preview with exact diffs and evidence
  -> exact owner checkpoint approval
  -> transactional apply with stale-baseline checks
  -> owner-workspace re-observation and independent reverification
  -> evidence-bound local Git branch/commit
  -> optional separately approved push/PR/publication
  -> postconditions, rollback availability, terminal result, and receipts
```

The model may propose content and parameters. It may never choose or expand
authority, forge current-state hashes, select an uninstalled recipe, waive
verification, approve a checkpoint, choose a secret, invent a Git remote, or
claim that an effect occurred.

### Vertical-slice implementation checklist

Continue from Handoff 0239. Preserve these pieces as one path:

1. A bounded, no-symlink candidate-context reader with explicit file, byte,
   depth, and document limits.
2. A strict versioned model-output contract for create, replace, move, delete,
   directory, text, and bounded asset proposals. Reject duplicate JSON keys,
   unknown fields, traversal, metadata roots, duplicate targets, invalid UTF-8,
   unsupported media, and over-budget output.
3. A bounded generation and repair coordinator using the production inference
   port. Persist generation intent and validated plans before candidate effects.
4. Core binding of every accepted operation to the current candidate entry,
   expected-before digest, task path rules, operation authority, and remaining
   monotonic budget.
5. Durable, replay-safe execution of the generated candidate operations through
   the existing candidate editing service.
6. A trusted selector over the installed signed recipe catalog. Model output
   must not provide recipe coordinates.
7. Candidate verification, bounded repair/escalation, and real changeset
   preview through typed receipts and the lifecycle driver.
8. Restart recovery for proposal activation, partial generation, partial edits,
   verification, and checkpoint preparation. Never mark a proposal `activated`
   before later failure becomes recoverable or truthfully terminal.
9. Conversation-first Console wiring and equivalent Shell commands showing the
   requested powers, budgets, candidate, progress, failures, test evidence,
   complete diff, rollback scope, and exact approval boundary.
10. Requalify transactional apply, re-observation, reverification, local Git
    delivery, and rollback from the next signed installed candidate; then add
    optional separately approved publication and the terminal result.

Current continuation status: steps 1--9 and step 10 through verified local Git
delivery are signed-installed for the ordinary single-repository path in
Handoff 0238. Exact history-preserving rollback and separately approved,
credential-opaque publication are source-composed in Handoffs 0239--0240, and
owner-encrypted natural failure incident attachment plus typed controls are
source-composed in Handoff 0241. Restart-safe automatic feature branching is
source-composed in Handoff 0242. Handoff 0243 packages all of these into signed
candidate `phase30-integrated-20260719-1`; 62 installed-package tests pass, but
the live verifier qualification remains fail-closed because `fam-os-userns` is
not loaded. A trusted independent-review passage gate is source-composed in
Handoff 0244; selection policy, a production reviewer, remediation receipt
resolution, and waiver ceremony remain open. A trusted generated-documentation
admission, traceability, and stale-apply gate is source-composed in Handoff
0245. Handoff 0246 packages release-signed selection and deterministic
generation for all five artifact kinds into healthy isolated candidate
`phase30-governance-20260719-3`; 74 installed-package tests pass with no
checkout import. Automatic stale regeneration, governance-file digest binding,
complete requirement traces, and live production-verifier proof remain open.
Handoff 0247 adds source-composed automatic incident evidence preservation and
diagnosis with immutable typed receipts, restart idempotence, read-only
Console/Shell visibility, and denial of fabricated or wrong-stage evidence.
Handoff 0248 now source-composes the post-apply failure branch through an exact
pre-commit rollback checkpoint, real restoration without a Git effect, typed
reporting, closure, retry reconciliation, and Console/Shell presentation.
Handoff 0249 now source-composes one bounded verification-driven repair,
final-state changeset squashing, sanitized diagnostic disclosure, typed
remediation, two ordered recovery observations, reporting, and closure.
Documentation-aware regeneration during repair and an independent later
recovery observation remained open at that checkpoint. Handoff 0250 now
source-composes persisted policy selection, digest-bound governance files,
automatic signed regeneration inside repair, current-receipt supersession, and
truthful requirement traces. The repaired branch and Handoff 0248 rollback
branch must both be included in the next signed candidate. Handoff 0252 now
keeps repair recovery monitored until a distinct signed post-apply
owner-workspace reverification records the second observation, report, and
closure. Installed/live proof remains open.
Handoff 0251 now source-composes deterministic code/security/architecture/design
selection, an Ed25519 release-signed independently identified reviewer, typed
remediation-plus-verification resolution receipts, and exact authenticated
owner waiver with truthful reduced assurance. Real integrations prove both
signed passage and a blocking security finding through waiver, apply,
reverification, and local commit. This is not yet signed-installed/live proof
and is not the separate Phase 31.5 independent human review.
Preserve and revalidate these paths; do not rebuild a parallel path. Next add
the remaining governance/auxiliary attachment and specialized powers, then
satisfy the host sandbox prerequisite and run clean-room/both-profile
qualification before claiming Phase 30 complete.

Do not route engineering requests back through the legacy four-existing-file
workspace patch path. Do not switch the UI to the new route until the route can
reach a real verified changeset checkpoint or return a truthful actionable
failure.

## Dependency-Ordered Completion Work

After the ordinary local vertical slice works, attach every remaining power to
the same authority, budget, candidate, verification, receipt, UI, and recovery
lifecycle.

### Phase 24 — Authority and contracts

Revalidate all fourteen owner-grantable authorities through the actual
installed path. Prove visible scope, expiry, revocation, restart
reconfirmation, one-use consumption, break-glass consequences, and truthful
unverified/waived labels. Component contracts alone are insufficient for final
completion.

### Phase 25 — Repository intelligence

Replace fake-only repository evidence in final qualification with real bounded
repository observations. Prove unfamiliar-repository mapping, implementation
trace, affected tests, architecture rules, untrusted instruction handling, and
no mutation before approval.

### Phase 26 — Candidate manipulation

Drive every typed file, directory, move, delete, executable, restoration, text,
binary, and self-source operation through the installed natural-language loop.
Prove stale state, races, cancellation, interruption recovery, rollback, and
preservation of unrelated owner changes.

### Phase 27 — Execution and operational engineering

Complete and wire:

- 27.11 runtime debugging, traces, dumps, CPU/memory profiling, race/leak
  detection, and exact-baseline performance regression;
- 27.12 database inspection, forward/rollback migrations, fixtures,
  transactional tests, backups, restore verification, compensation, and the
  remaining declared database-engine coverage;
- 27.13 services, APIs, real browsers, containers, mixed/local clusters,
  ports, volumes, health, cleanup, retained artifacts, and enforced egress;
  preserve the exact API/static candidate/post-apply receipts and versioned
  graph from ADRs 0221–0223 and the distinct exact resource ceremony from ADR
  0224 while extending the same planner and lifecycle to additional declared
  environment kinds;
- 27.14 systemd, container, Kubernetes, and IaC plan/apply/monitor/rollback
  adapters;
- 27.15 reproducible artifacts, signing, provenance, SBOM attachment,
  registry publication, promotion, revocation, and release rollback;
- 27.16 secret references, opaque injection, redaction, explicit disclosure,
  rotation, dependent-service restart, verification, and revocation.

Do not mark a requirement complete because one backend exists if its wording
requires multiple declared backends or an installed matrix.

### Phase 28 — Design

Wire design briefs, design-system generation, raster/vector/media operations,
sanitization, accessibility, responsive capture, visual regression, preview,
apply, and rollback into the same natural-language task lifecycle. Requalify
the final installed path rather than only the design fixture.

### Phase 29 — Git and delivery

Revalidate 29.1–29.6 through the master loop and complete:

- 29.7 fetch, review, merge, rebase, conflicts, tags, releases, and remote-state
  reconciliation with exact old/new objects and recovery refs;
- 29.8 ordered multi-repository delivery, compatibility verification,
  per-repository checkpoints, partial-publication compensation, and resumable
  receipts.

No credential material may enter model context, evidence, logs, or candidate
workspaces. Publication remains a separate exact approval unless the owner has
deliberately granted the corresponding bounded authority.

### Phase 30 — Full orchestration and governance

Complete 30.1 and 30.5–30.9. Preserve the installed signed documentation
generation path, add governance-file digest binding, automatic stale
regeneration, and complete trace creation. Preserve typed automatic incident
preservation/diagnosis and wire real remediation, monitored recovery, rollback,
reporting, and closure producers into that same chain. Preserve Handoff 0248's
real rollback/report/closure branch and add the repair plus monitored-recovery
branch from Handoff 0249 and preserve Handoff 0250's documentation-aware repair
regeneration and Handoff 0252's independently later recovery observation. Wire
the review evidence into the next signed installed candidate and preserve its
strict selection, resolution, and waiver boundaries. Console and Shell must
expose real state and actions, not projections backed only by test-generated
receipts. Exercise multiple checkpoints under one monotonic budget and prove
restart never reuses mutation or publication confirmation.

### Phase 31 — Final qualification

Only after the complete source path is frozen:

1. Run focused tests while implementing each behavior.
2. Run all affected unit, contract, integration, security, and architecture
   suites.
3. Render and validate every schema.
4. Run the complete source suite.
5. Build a clean wheel and signed release candidate.
6. Install it without checkout import leakage.
7. Exercise the real Console and Shell natural-language lifecycle against
   greenfield creation, feature work, refactoring, bug repair, architecture
   migration, UI redesign, database migration, service integration,
   deployment preview, rollback, Git delivery, and optional draft PR.
8. Rerun every supported language, dependency, design, candidate, Git,
   restart, self-hosted source, adversarial, lifecycle, fresh-user,
   update/rollback/repair/removal, and hardware-profile scenario from that exact
   candidate.
9. Obtain owner authentication to load required host security policy; do not
   replace required host enforcement with a mock.
10. Run the full minimum 24-hour mixed pressure soak from zero on the exact
    final candidate after all prerequisites pass.
11. Obtain the independent human security review. The implementing agent may
    prepare evidence and remediation but may not impersonate the reviewer.
12. Rerun qualification after any post-review code change that affects the
    reviewed or qualified surface.
13. Update `configs/integration/coverage.json` only from direct final-candidate
    evidence.

## Work Cycle

For every coherent behavior change:

1. Identify the exact requirement and authoritative completion evidence.
2. Use Larry and the code graph to locate the existing boundary.
3. Add or update typed contracts before concrete effects.
4. Add a failing regression, hostile fixture, or acceptance test when feasible.
5. Implement through the correct unprivileged Core/adapters/Supervisor
   boundary.
6. Run focused tests.
7. Run affected contract, schema, security, integration, and architecture
   tests.
8. Exercise the production composition path.
9. Preserve failures as named machine-readable evidence.
10. Update or add an ADR for durable decisions.
11. Create the next append-only handoff for every major change.
12. Update plan status only after the requirement's full exit condition passes.
13. Continue automatically to the next unproven requirement.

Keep implementation files focused, normally below 300 lines, and functions
normally below 50 lines. Do not create a god orchestrator; use typed lifecycle
coordinators with replaceable ports.

## Required Mutation and Execution Tests

Every new mutation path must prove:

- no effect before exact approval;
- current-state and stale-baseline checks;
- cancellation before and during effect;
- partial-failure compensation;
- restart reconciliation without replay;
- revocation and expiry;
- symlink, hardlink, traversal, and race rejection;
- exact audit and postcondition evidence;
- complete rollback or explicit irreversibility;
- preservation of unrelated user work.

Every execution path must prove:

- signed recipe identity and trusted selection;
- exact executable/toolchain binding;
- cgroup, time, memory, process, output, artifact, and storage bounds;
- sanitized environment and no host home;
- network denied or exact broker-enforced allowlist;
- secret isolation and redaction;
- complete process-tree termination;
- positive and deliberately failing fixtures;
- failed verification can never advance as verified.

## Status Discipline

Use these maturity labels precisely:

- `contract_only`
- `component_tested`
- `source_composed`
- `production_wired`
- `installed_tested`
- `operationally_proven`

Never collapse them into “done.” In every progress report, state the exact
maturity and evidence location.

Do not check a later numbered requirement merely because a supporting component
exists. When a requirement says “installed,” “production-reachable,” “all,”
“complete,” or names multiple backends/scenarios, its evidence must match that
scope exactly.

If an existing checkmark is contradicted by current evidence, reopen it or add
an explicit corrective status note. Do not preserve an inaccurate status for
appearance.

## Blockers

External requirements such as administrator authentication, a second physical
machine, a 24-hour elapsed run, or an independent human reviewer may remain
open only when genuinely unavailable.

For each blocker:

1. Record the exact command, result, prerequisite, and responsible external
   action.
2. Complete all independent implementation, tests, packaging, runbooks, and
   qualification preparation.
3. Continue all other unblocked work.
4. Never lower, rename, or simulate the gate.
5. Ask the owner only when their action is actually required and no other
   meaningful in-scope work remains on that dependency.

Known external gates include loading the required `fam-os-userns` host profile,
running any truly distinct physical-host qualification required by the plans,
the uninterrupted final 24-hour soak, and independent human security review.

## Completion Audit

Before claiming the goal complete, build a requirement-to-evidence matrix for:

- every Phase 24–31 numbered requirement;
- every Phase 24–31 exit gate;
- every test and acceptance requirement in `MASTER_PLANv2.md`;
- all still-open Phase 0–23 dependencies referenced by v2;
- every public contract and schema;
- every production composition root;
- Console and Shell parity;
- every supported language, backend, authority, and hardware profile;
- fresh install, update, rollback, diagnosis, repair, and complete removal;
- soak and human-review results;
- every integration-coverage row.

For each row, cite direct current evidence and classify it as proven,
contradicted, incomplete, indirect, or missing. Anything other than proven
remains open.

The goal is complete only when:

- the installed natural-language lifecycle reaches verified checkpoint, apply,
  reverification, Git delivery, and optional publication;
- all eighteen currently open requirements are complete;
- every checked requirement has been revalidated where new wiring could affect
  it;
- every exit gate passes from the exact final signed candidate;
- all required physical, long-running, lifecycle, and human-review evidence is
  present;
- integration coverage truthfully reports every required subsystem as
  `operationally_proven` from direct evidence;
- no required work remains hidden in TODOs, comments, stale docs, unsupported
  claims, test fakes, or implied follow-up.

Only then mark the persistent goal complete. Until then, keep working or leave
the exact external gate visibly open without claiming overall completion.
