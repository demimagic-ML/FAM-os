# Widget demo

The release screenshot and live widget contract are documented in
`docs/installation/omarchy.md`. The plugin intentionally contains no mock data:
its demo is the authenticated local `fam.widget/v1` endpoint exposed by a
running FAM_OS service.
