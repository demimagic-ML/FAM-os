{
  "$defs": {
    "ShellCancelCommand": {
      "additionalProperties": false,
      "properties": {
        "expected_revision": {
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "expected_revision"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.shell.cancel/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.shell/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ShellCancelCommand"
    },
    "schema_id": {
      "const": "fam.shell.cancel/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "FAM Shell cancellation command",
  "type": "object"
}
