{
  "$defs": {
    "ShellEngineeringContextRequest": {
      "additionalProperties": false,
      "properties": {
        "authority_session_id": {
          "type": "string"
        },
        "confirmed": {
          "type": "boolean"
        },
        "contract_version": {
          "const": "fam.shell.engineering-authority/v1alpha1"
        },
        "owner_id": {
          "type": "string"
        },
        "payload_sha256": {
          "type": "string"
        },
        "purpose": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        }
      },
      "required": [
        "request_id",
        "authority_session_id",
        "owner_id",
        "purpose",
        "payload_sha256",
        "confirmed",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.shell.engineering-context/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.shell.engineering-authority/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ShellEngineeringContextRequest"
    },
    "schema_id": {
      "const": "fam.shell.engineering-context/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Session-bound owner engineering authentication context request",
  "type": "object"
}
