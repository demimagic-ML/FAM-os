{
  "$defs": {
    "MemoryExpiryEvaluation": {
      "additionalProperties": false,
      "properties": {
        "contract_version": {
          "const": "fam.memory.lifecycle/v1alpha1"
        },
        "evaluated_at": {
          "format": "date-time",
          "type": "string"
        },
        "expires_at": {
          "format": "date-time",
          "type": "string"
        },
        "record_id": {
          "type": "string"
        },
        "state": {
          "enum": [
            "active",
            "expired",
            "purged"
          ],
          "type": "string"
        }
      },
      "required": [
        "record_id",
        "expires_at",
        "evaluated_at",
        "state",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.memory.expiry-evaluation/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.memory.lifecycle/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/MemoryExpiryEvaluation"
    },
    "schema_id": {
      "const": "fam.memory.expiry-evaluation/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Memory expiry evaluation",
  "type": "object"
}
