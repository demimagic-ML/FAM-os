{
  "$defs": {
    "EngineeringCapabilityUnavailable": {
      "additionalProperties": false,
      "properties": {
        "capability_id": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "reason_code": {
          "type": "string"
        },
        "recorded_at": {
          "format": "date-time",
          "type": "string"
        },
        "required_authorities": {
          "items": {
            "enum": [
              "observe",
              "propose",
              "modify",
              "execute",
              "network",
              "publish",
              "raw_shell",
              "host_admin",
              "secret_use",
              "global_install",
              "production_mutate",
              "policy_change",
              "protected_ref_write",
              "self_update"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "result_id": {
          "type": "string"
        },
        "result_kind": {
          "enum": [
            "changeset_proposal",
            "verified_changeset_receipt",
            "publication_proposal",
            "publication_receipt",
            "capability_unavailable",
            "engineering_execution"
          ],
          "type": "string"
        },
        "retryable_after_owner_action": {
          "type": "boolean"
        },
        "safe_message": {
          "type": "string"
        },
        "task_id": {
          "type": "string"
        }
      },
      "required": [
        "result_id",
        "task_id",
        "recorded_at",
        "capability_id",
        "required_authorities",
        "reason_code",
        "safe_message",
        "retryable_after_owner_action",
        "result_kind",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.engineering-unavailable/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/EngineeringCapabilityUnavailable"
    },
    "schema_id": {
      "const": "fam.core.engineering-unavailable/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Unavailable engineering capability result",
  "type": "object"
}
