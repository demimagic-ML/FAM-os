{
  "$defs": {
    "ToolRecipe": {
      "additionalProperties": false,
      "properties": {
        "argv": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "environment_keys": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "expected_exit_codes": {
          "items": {
            "type": "integer"
          },
          "type": "array"
        },
        "network_required": {
          "type": "boolean"
        },
        "recipe_id": {
          "type": "string"
        },
        "required_authorities": {
          "items": {
            "enum": [
              "observe",
              "propose",
              "modify",
              "execute",
              "network",
              "publish",
              "raw_shell",
              "host_admin",
              "secret_use",
              "global_install",
              "production_mutate",
              "policy_change",
              "protected_ref_write",
              "self_update"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "task_id": {
          "type": "string"
        },
        "timeout_seconds": {
          "type": "integer"
        },
        "verifier_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "working_directory": {
          "type": "string"
        }
      },
      "required": [
        "recipe_id",
        "task_id",
        "argv",
        "working_directory",
        "environment_keys",
        "timeout_seconds",
        "network_required",
        "required_authorities",
        "expected_exit_codes",
        "verifier_ids",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.tool-recipe/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ToolRecipe"
    },
    "schema_id": {
      "const": "fam.core.tool-recipe/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Authorized deterministic tool recipe",
  "type": "object"
}
