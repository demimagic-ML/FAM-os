{
  "$defs": {
    "SecretUseReceipt": {
      "additionalProperties": false,
      "properties": {
        "authorization_id": {
          "type": "string"
        },
        "consumer_id": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "level": {
          "enum": [
            "opaque_injection",
            "redacted_transformation",
            "direct_model_disclosure"
          ],
          "type": "string"
        },
        "output_sha256": {
          "type": "string"
        },
        "plaintext_persisted": {
          "type": "boolean"
        },
        "receipt_id": {
          "type": "string"
        },
        "redaction_evidence_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "secret_ref": {
          "type": "string"
        },
        "secret_value_logged": {
          "type": "boolean"
        },
        "used_at": {
          "format": "date-time",
          "type": "string"
        }
      },
      "required": [
        "receipt_id",
        "authorization_id",
        "secret_ref",
        "consumer_id",
        "level",
        "used_at",
        "output_sha256",
        "redaction_evidence_id",
        "plaintext_persisted",
        "secret_value_logged",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.secret-use-receipt/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/SecretUseReceipt"
    },
    "schema_id": {
      "const": "fam.core.secret-use-receipt/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Redaction-safe secret use receipt",
  "type": "object"
}
