{
  "$defs": {
    "PeerEndpoint": {
      "additionalProperties": false,
      "properties": {
        "host": {
          "type": "string"
        },
        "port": {
          "type": "integer"
        }
      },
      "required": [
        "host",
        "port"
      ],
      "type": "object"
    },
    "PeerServiceConfiguration": {
      "additionalProperties": false,
      "properties": {
        "advertised_endpoint": {
          "anyOf": [
            {
              "$ref": "#/$defs/PeerEndpoint"
            },
            {
              "type": "null"
            }
          ]
        },
        "contract_version": {
          "const": "fam.fabric.peer-service-configuration/v1alpha1"
        },
        "display_name": {
          "type": "string"
        },
        "enabled": {
          "type": "boolean"
        },
        "listen_host": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "listen_port": {
          "type": "integer"
        }
      },
      "required": [
        "enabled",
        "display_name",
        "listen_host",
        "listen_port",
        "advertised_endpoint",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.fabric.peer-service-configuration/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.fabric.peer-service-configuration/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/PeerServiceConfiguration"
    },
    "schema_id": {
      "const": "fam.fabric.peer-service-configuration/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Installed peer listener configuration",
  "type": "object"
}
