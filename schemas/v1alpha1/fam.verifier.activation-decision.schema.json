{
  "$defs": {
    "VerifierActivationDecision": {
      "additionalProperties": false,
      "properties": {
        "allowed": {
          "type": "boolean"
        },
        "contract_version": {
          "const": "fam.verifier.activation-decision/v1alpha1"
        },
        "package_id": {
          "type": "string"
        },
        "package_version": {
          "type": "string"
        },
        "policy_id": {
          "type": "string"
        },
        "reason_code": {
          "type": "string"
        },
        "verified_artifact_digest": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "verifier_id": {
          "type": "string"
        }
      },
      "required": [
        "allowed",
        "reason_code",
        "verifier_id",
        "package_id",
        "package_version",
        "policy_id",
        "verified_artifact_digest",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.verifier.activation-decision/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.verifier.activation-decision/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/VerifierActivationDecision"
    },
    "schema_id": {
      "const": "fam.verifier.activation-decision/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Verifier activation decision",
  "type": "object"
}
