{
  "$defs": {
    "SecretUseAuthorization": {
      "additionalProperties": false,
      "properties": {
        "approved_consumer_id": {
          "type": "string"
        },
        "authorization_id": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "direct_disclosure_consequences_sha256": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "expires_at": {
          "format": "date-time",
          "type": "string"
        },
        "grant_id": {
          "type": "string"
        },
        "issued_at": {
          "format": "date-time",
          "type": "string"
        },
        "level": {
          "enum": [
            "opaque_injection",
            "redacted_transformation",
            "direct_model_disclosure"
          ],
          "type": "string"
        },
        "maximum_uses": {
          "type": "integer"
        },
        "owner_id": {
          "type": "string"
        },
        "principal_id": {
          "type": "string"
        },
        "purpose": {
          "type": "string"
        },
        "required_authority": {
          "enum": [
            "observe",
            "propose",
            "modify",
            "execute",
            "network",
            "publish",
            "raw_shell",
            "host_admin",
            "secret_use",
            "global_install",
            "production_mutate",
            "policy_change",
            "protected_ref_write",
            "self_update"
          ],
          "type": "string"
        },
        "secret_ref": {
          "type": "string"
        },
        "task_id": {
          "type": "string"
        }
      },
      "required": [
        "authorization_id",
        "task_id",
        "grant_id",
        "owner_id",
        "principal_id",
        "secret_ref",
        "level",
        "approved_consumer_id",
        "purpose",
        "issued_at",
        "expires_at",
        "maximum_uses",
        "direct_disclosure_consequences_sha256",
        "required_authority",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.secret-use-authorization/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/SecretUseAuthorization"
    },
    "schema_id": {
      "const": "fam.core.secret-use-authorization/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Tiered exact secret-use authorization",
  "type": "object"
}
