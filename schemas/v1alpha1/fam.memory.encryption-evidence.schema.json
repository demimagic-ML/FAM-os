{
  "$defs": {
    "MemoryEncryptionEvidence": {
      "additionalProperties": false,
      "properties": {
        "algorithm": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.memory.encryption/v1alpha1"
        },
        "cross_owner_rejected": {
          "type": "boolean"
        },
        "encrypted_database_sha256": {
          "type": "string"
        },
        "evidence_id": {
          "type": "string"
        },
        "owner_round_trip": {
          "type": "boolean"
        },
        "passed": {
          "type": "boolean"
        },
        "plaintext_absent": {
          "type": "boolean"
        }
      },
      "required": [
        "evidence_id",
        "algorithm",
        "plaintext_absent",
        "owner_round_trip",
        "cross_owner_rejected",
        "encrypted_database_sha256",
        "passed",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.memory.encryption-evidence/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.memory.encryption/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/MemoryEncryptionEvidence"
    },
    "schema_id": {
      "const": "fam.memory.encryption-evidence/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Owner-isolated memory encryption evidence",
  "type": "object"
}
