{
  "$defs": {
    "LiveAdaptationControlRequest": {
      "additionalProperties": false,
      "properties": {
        "confirmed": {
          "type": "boolean"
        },
        "contract_version": {
          "const": "fam.adaptation.live-control/v1alpha1"
        },
        "operation": {
          "enum": [
            "enable",
            "disable",
            "reset",
            "evaluate",
            "rollback"
          ],
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "target_workflow_id": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "request_id",
        "operation",
        "confirmed",
        "target_workflow_id",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.adaptation.control-request/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.adaptation.live-control/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/LiveAdaptationControlRequest"
    },
    "schema_id": {
      "const": "fam.adaptation.control-request/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Confirmed live adaptation control request",
  "type": "object"
}
