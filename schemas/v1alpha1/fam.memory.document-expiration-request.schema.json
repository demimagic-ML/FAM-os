{
  "$defs": {
    "DocumentExpirationRequest": {
      "additionalProperties": false,
      "properties": {
        "confirmed": {
          "type": "boolean"
        },
        "contract_version": {
          "const": "fam.memory.management/v1alpha1"
        },
        "grant_id": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        }
      },
      "required": [
        "request_id",
        "grant_id",
        "confirmed",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.memory.document-expiration-request/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.memory.management/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/DocumentExpirationRequest"
    },
    "schema_id": {
      "const": "fam.memory.document-expiration-request/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Confirmed document expiration request",
  "type": "object"
}
