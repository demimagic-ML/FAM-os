{
  "$defs": {
    "CheckpointDecision": {
      "additionalProperties": false,
      "properties": {
        "checkpoint_id": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "decided_at": {
          "format": "date-time",
          "type": "string"
        },
        "decided_by": {
          "type": "string"
        },
        "decision_id": {
          "type": "string"
        },
        "disposition": {
          "enum": [
            "approved",
            "denied",
            "expired"
          ],
          "type": "string"
        },
        "proposal_id": {
          "type": "string"
        },
        "proposal_sha256": {
          "type": "string"
        },
        "reason": {
          "type": "string"
        },
        "task_id": {
          "type": "string"
        }
      },
      "required": [
        "decision_id",
        "task_id",
        "proposal_id",
        "checkpoint_id",
        "decided_by",
        "decided_at",
        "disposition",
        "proposal_sha256",
        "reason",
        "contract_version"
      ],
      "type": "object"
    },
    "ShellEngineeringChangesetApplyRequest": {
      "additionalProperties": false,
      "properties": {
        "changeset_id": {
          "type": "string"
        },
        "confirmed": {
          "type": "boolean"
        },
        "contract_version": {
          "const": "fam.shell.engineering-loop/v1alpha1"
        },
        "decision": {
          "$ref": "#/$defs/CheckpointDecision"
        },
        "owner_id": {
          "type": "string"
        },
        "principal_id": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "session_id": {
          "type": "string"
        },
        "task_id": {
          "type": "string"
        }
      },
      "required": [
        "request_id",
        "owner_id",
        "task_id",
        "changeset_id",
        "decision",
        "session_id",
        "principal_id",
        "confirmed",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.shell.engineering-changeset-apply/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.shell.engineering-loop/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ShellEngineeringChangesetApplyRequest"
    },
    "schema_id": {
      "const": "fam.shell.engineering-changeset-apply/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Exact owner-approved candidate changeset apply request",
  "type": "object"
}
