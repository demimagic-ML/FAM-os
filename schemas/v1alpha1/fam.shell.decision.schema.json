{
  "$defs": {
    "ShellDecisionCommand": {
      "additionalProperties": false,
      "properties": {
        "approval_id": {
          "type": "string"
        },
        "decision": {
          "enum": [
            "approve",
            "deny"
          ],
          "type": "string"
        },
        "expected_revision": {
          "type": "integer"
        },
        "session_id": {
          "type": "string"
        }
      },
      "required": [
        "session_id",
        "expected_revision",
        "approval_id",
        "decision"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.shell.decision/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.shell/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ShellDecisionCommand"
    },
    "schema_id": {
      "const": "fam.shell.decision/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "FAM Shell decision command",
  "type": "object"
}
