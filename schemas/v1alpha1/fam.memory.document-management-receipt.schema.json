{
  "$defs": {
    "DocumentManagementReceipt": {
      "additionalProperties": false,
      "properties": {
        "affected_document_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "contract_version": {
          "const": "fam.memory.management/v1alpha1"
        },
        "operation": {
          "enum": [
            "correct",
            "expire",
            "delete"
          ],
          "type": "string"
        },
        "owner_id": {
          "type": "string"
        },
        "payload_removed": {
          "type": "boolean"
        },
        "performed_at": {
          "format": "date-time",
          "type": "string"
        },
        "performed_by": {
          "type": "string"
        },
        "previous_content_sha256": {
          "type": "string"
        },
        "receipt_id": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "resulting_content_sha256": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        },
        "target_id": {
          "type": "string"
        },
        "tombstone_sha256": {
          "type": "string"
        }
      },
      "required": [
        "receipt_id",
        "request_id",
        "operation",
        "target_id",
        "owner_id",
        "performed_by",
        "performed_at",
        "previous_content_sha256",
        "resulting_content_sha256",
        "affected_document_ids",
        "tombstone_sha256",
        "payload_removed",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.memory.document-management-receipt/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.memory.management/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/DocumentManagementReceipt"
    },
    "schema_id": {
      "const": "fam.memory.document-management-receipt/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Durable document management receipt",
  "type": "object"
}
