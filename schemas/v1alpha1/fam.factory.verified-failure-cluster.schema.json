{
  "$defs": {
    "VerifiedFailureCluster": {
      "additionalProperties": false,
      "properties": {
        "capability_id": {
          "type": "string"
        },
        "cluster_id": {
          "type": "string"
        },
        "cluster_sha256": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.factory.failure-discovery/v1alpha1"
        },
        "evidence_sha256s": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "failed_requirement_id": {
          "type": "string"
        },
        "family_id": {
          "type": "string"
        },
        "first_observed_at": {
          "format": "date-time",
          "type": "string"
        },
        "last_observed_at": {
          "format": "date-time",
          "type": "string"
        },
        "trace_ids": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "verifier_id": {
          "type": "string"
        }
      },
      "required": [
        "cluster_id",
        "family_id",
        "capability_id",
        "failed_requirement_id",
        "verifier_id",
        "trace_ids",
        "evidence_sha256s",
        "first_observed_at",
        "last_observed_at",
        "cluster_sha256",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.factory.verified-failure-cluster/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.factory.failure-discovery/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/VerifiedFailureCluster"
    },
    "schema_id": {
      "const": "fam.factory.verified-failure-cluster/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Immutable production failure cluster",
  "type": "object"
}
