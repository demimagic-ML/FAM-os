{
  "$defs": {
    "IntegrationNetworkAttachment": {
      "additionalProperties": false,
      "properties": {
        "attachment_reference": {
          "type": "string"
        },
        "kind": {
          "enum": [
            "linux_namespace",
            "docker_internal_network"
          ],
          "type": "string"
        },
        "proxy_uri": {
          "type": "string"
        }
      },
      "required": [
        "kind",
        "attachment_reference",
        "proxy_uri"
      ],
      "type": "object"
    },
    "IntegrationNetworkLease": {
      "additionalProperties": false,
      "properties": {
        "attachments": {
          "items": {
            "$ref": "#/$defs/IntegrationNetworkAttachment"
          },
          "type": "array"
        },
        "authority_ref": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "destinations": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enforcement_id": {
          "type": "string"
        },
        "environment_id": {
          "type": "string"
        },
        "evidence_sha256": {
          "type": "string"
        },
        "expires_at": {
          "format": "date-time",
          "type": "string"
        },
        "issued_at": {
          "format": "date-time",
          "type": "string"
        },
        "maximum_network_bytes": {
          "type": "integer"
        },
        "principal_id": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "session_id": {
          "type": "string"
        }
      },
      "required": [
        "enforcement_id",
        "request_id",
        "environment_id",
        "principal_id",
        "session_id",
        "authority_ref",
        "attachments",
        "destinations",
        "maximum_network_bytes",
        "issued_at",
        "expires_at",
        "evidence_sha256",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.integration-network-lease/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/IntegrationNetworkLease"
    },
    "schema_id": {
      "const": "fam.core.integration-network-lease/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Broker-owned integration network attachment lease",
  "type": "object"
}
