{
  "$defs": {
    "EngineeringAuthorizationDecision": {
      "additionalProperties": false,
      "properties": {
        "allowed": {
          "type": "boolean"
        },
        "authority": {
          "enum": [
            "observe",
            "propose",
            "modify",
            "execute",
            "network",
            "publish",
            "raw_shell",
            "host_admin",
            "secret_use",
            "global_install",
            "production_mutate",
            "policy_change",
            "protected_ref_write",
            "self_update"
          ],
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "decided_at": {
          "format": "date-time",
          "type": "string"
        },
        "decision_id": {
          "type": "string"
        },
        "grant_id": {
          "type": "string"
        },
        "reason_code": {
          "type": "string"
        },
        "request_id": {
          "type": "string"
        }
      },
      "required": [
        "decision_id",
        "request_id",
        "grant_id",
        "authority",
        "decided_at",
        "allowed",
        "reason_code",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.engineering-authorization-decision/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/EngineeringAuthorizationDecision"
    },
    "schema_id": {
      "const": "fam.core.engineering-authorization-decision/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Deterministic engineering authorization decision",
  "type": "object"
}
