{
  "$defs": {
    "ShellEngineeringSecretQuery": {
      "additionalProperties": false,
      "properties": {
        "contract_version": {
          "const": "fam.shell.engineering-secret/v1alpha1"
        },
        "operation": {
          "enum": [
            "list",
            "inspect",
            "audit",
            "provision",
            "rotate",
            "delete"
          ],
          "type": "string"
        },
        "request_id": {
          "type": "string"
        },
        "secret_ref": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "request_id",
        "operation",
        "secret_ref",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.shell.engineering-secret-query/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.shell.engineering-secret/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/ShellEngineeringSecretQuery"
    },
    "schema_id": {
      "const": "fam.shell.engineering-secret-query/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Metadata-only owner engineering secret query",
  "type": "object"
}
