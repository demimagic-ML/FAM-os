{
  "$defs": {
    "BreakGlassDecision": {
      "additionalProperties": false,
      "properties": {
        "authentication_context_id": {
          "type": "string"
        },
        "challenge_id": {
          "type": "string"
        },
        "consequences_sha256": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "decided_at": {
          "format": "date-time",
          "type": "string"
        },
        "decision_id": {
          "type": "string"
        },
        "disposition": {
          "enum": [
            "approved",
            "denied"
          ],
          "type": "string"
        },
        "grant_id": {
          "type": "string"
        },
        "owner_id": {
          "type": "string"
        },
        "scope_id": {
          "type": "string"
        },
        "scope_kind": {
          "enum": [
            "action",
            "changeset",
            "task",
            "session"
          ],
          "type": "string"
        }
      },
      "required": [
        "decision_id",
        "challenge_id",
        "owner_id",
        "grant_id",
        "disposition",
        "scope_kind",
        "scope_id",
        "consequences_sha256",
        "decided_at",
        "authentication_context_id",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.engineering-break-glass-decision/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/BreakGlassDecision"
    },
    "schema_id": {
      "const": "fam.core.engineering-break-glass-decision/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Authenticated engineering break-glass decision",
  "type": "object"
}
