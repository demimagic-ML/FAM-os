{
  "$defs": {
    "BreakGlassChallenge": {
      "additionalProperties": false,
      "properties": {
        "authorities": {
          "items": {
            "enum": [
              "observe",
              "propose",
              "modify",
              "execute",
              "network",
              "publish",
              "raw_shell",
              "host_admin",
              "secret_use",
              "global_install",
              "production_mutate",
              "policy_change",
              "protected_ref_write",
              "self_update"
            ],
            "type": "string"
          },
          "type": "array"
        },
        "challenge_id": {
          "type": "string"
        },
        "consequences": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "consequences_sha256": {
          "type": "string"
        },
        "contract_version": {
          "const": "fam.core.engineering/v1alpha1"
        },
        "expires_at": {
          "format": "date-time",
          "type": "string"
        },
        "grant_id": {
          "type": "string"
        },
        "issued_at": {
          "format": "date-time",
          "type": "string"
        },
        "owner_id": {
          "type": "string"
        },
        "scope_id": {
          "type": "string"
        },
        "scope_kind": {
          "enum": [
            "action",
            "changeset",
            "task",
            "session"
          ],
          "type": "string"
        },
        "verification": {
          "enum": [
            "required",
            "allow_unverified",
            "waived"
          ],
          "type": "string"
        }
      },
      "required": [
        "challenge_id",
        "owner_id",
        "grant_id",
        "authorities",
        "verification",
        "scope_kind",
        "scope_id",
        "consequences",
        "consequences_sha256",
        "issued_at",
        "expires_at",
        "contract_version"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.fam-os.local/fam.core.engineering-break-glass/v1alpha1.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "contract_version": {
      "const": "fam.core.engineering/v1alpha1"
    },
    "payload": {
      "$ref": "#/$defs/BreakGlassChallenge"
    },
    "schema_id": {
      "const": "fam.core.engineering-break-glass/v1alpha1"
    }
  },
  "required": [
    "schema_id",
    "contract_version",
    "payload"
  ],
  "title": "Exact-consequence engineering break-glass challenge",
  "type": "object"
}
