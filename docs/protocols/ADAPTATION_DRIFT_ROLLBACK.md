# Adaptation drift and rollback

Phase 11.5 compares immutable digest-bound snapshots. Any verification-quality loss is drift. Latency or energy above 110% of baseline is also drift. Rollback restores the exact accepted snapshot digest and retains the rejected snapshot as evidence.
