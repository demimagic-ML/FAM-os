# Python verifier ownership

Owns extraction of generated Python, the trusted AST allow policy, trusted deterministic test bundles, script assembly, and verdict conversion over the sandbox port.

Candidate code and model claims are untrusted. Test bundles are verifier-owned trusted artifacts selected outside the candidate. This package does not choose repair prompts, escalation policy, persistence, or the operating-system sandbox mechanism.
